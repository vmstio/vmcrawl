[project]
name = "vmcrawl"
version = "0.3"
description = "Mastodon-focused version reporting crawler"
requires-python = ">=3.13"

dependencies = [
    "dnspython",
    "httpx[http2]",
    "beautifulsoup4",
    "python-dotenv",
    "toml",
    "lxml",
    "re",
    "packaging",
]